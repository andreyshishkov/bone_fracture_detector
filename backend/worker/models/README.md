# Предобученные модели

Складывайте сюда свои обученные модели (например, ultralytics `.pt`).

Имена файлов можно выбирать сами — при реализации задачи детекции путь к каталогу будет задаваться через конфиг (например, `WORKER_MODELS_DIR` или по типу модели: `nano`, `large`).
